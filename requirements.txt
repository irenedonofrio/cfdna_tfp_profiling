# Python packages imported by the pipeline scripts.
# Latest PyPI releases as of 2026-09-03. Requires Python >=3.12
# (numpy 2.5+ and scipy 1.18+ dropped 3.11).
#
# CLI tools used by the .sh scripts (gawk, samtools, GNU parallel) are
# NOT listed here — install them from the OS or conda (see README).
numpy>=2.5.2
pandas>=3.0.5
scipy>=1.18.1
pysam>=0.24.0
pyyaml>=6.0.3
matplotlib>=3.11.1
